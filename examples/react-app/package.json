{
	"name": "example-react-app",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "bun build --minify --outdir=dist --metafile=metafile.json ./src/index.tsx",
		"test:e2e": "bun run build && bun --eval 'import { verify } from \"../shared/verify\"; verify({ bundleName: \"react-app\", bundler: { name: \"bun\", version: \"1.2.0\" }, expectedModule: \"react\" })'"
	},
	"dependencies": {
		"react": "19.2.4",
		"react-dom": "19.2.4"
	}
}
