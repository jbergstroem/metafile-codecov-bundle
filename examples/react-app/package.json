{
	"name": "example-react-app",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"build": "bun build --minify --outdir=dist --metafile=metafile.json ./src/index.tsx",
		"test:e2e": "bun run build && bun run verify.ts"
	},
	"dependencies": {
		"metafile-codecov-bundle": "workspace:*",
		"react": "19.2.4",
		"react-dom": "19.2.4"
	}
}
